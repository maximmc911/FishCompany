В папке DeleteNotUser хранятся файлы, 
которые в качестве примера используются и/или в которых нет необходдимости на данном этапе разработки

Содержание:
1) components
    - formInput.tsx (файл переработан => 
    src\components\UI\formField\FormField.tsx
     )
     -useful_things.ts ( 
        а) Конвентор текста в html start

     )
2) tabs 
    - Tabs.tsx ( на данный момент используется только в одном компоненте, на данный момент нет необходимости использовать повторно
    Используется: src\Pages\main\Pages\useful\Useful.tsx
    )

3) textWord
    - index.tsx (пример использования фреймворка  ReactQuill для интеграции в проект расширяемого текстового редактора)

4) interface
    - index.ts (Интерфейсы, которые в процессе разработки потеряли актуальность)
